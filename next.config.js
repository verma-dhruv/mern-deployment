/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['in.bmscdn.com', 'assets-in.bmscdn.com', 'images.unsplash.com'], // Added 'images.unsplash.com'
    },
}

module.exports = nextConfig